/* ==========================================================================
   virgule. — site-config.js
   --------------------------------------------------------------------------
   Ce fichier est généré par le panneau /admin (bouton « exporter »).
   Il permet de publier tes réglages (textes, portrait, contact) :
   remplace ce fichier par celui téléchargé depuis le panneau.

   Tant qu'il vaut null, le site utilise les textes écrits dans index.html
   et, dans TON navigateur, les réglages enregistrés via /admin.
   ========================================================================== */
window.VIRGULE_CONFIG = null;
